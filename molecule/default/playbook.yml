---
- name: Converge
  hosts: all
  gather_facts: no
  strategy: free

  roles:
    - role: robertdebock.bootstrap
    - role: ansible-role-ruby

  tasks:
    - name: install a gem
      gem:
        name: vagrant
