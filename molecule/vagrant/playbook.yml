---
- name: Converge
  hosts: all
  become: true
  gather_facts: false

  roles:
    - role: robertdebock.bootstrap
    - role: ansible-role-ruby

  tasks:
    - name: install a gem
      gem:
        name: vagrant
